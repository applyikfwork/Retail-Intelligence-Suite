{
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && pnpm run typecheck:libs && pnpm --filter @workspace/omnistore-ai run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
